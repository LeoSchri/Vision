@{
    ViewBag.Title = "Pick";
}

<div class="border">
    <div class="container title">
        <div class="row padding-5">
            <div class="col-lg-12">Choose from following @RenderSection("Title",true)</div>
        </div>
    </div>

    <div class="container top-margin-10 padding-bottom-10">
        @RenderSection("Table", true)
    </div>
</div>
